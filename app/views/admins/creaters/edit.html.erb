<div class="container">
	<div class="row">
		<div class="col-lg-12">
			<%= form_for(@creater, url:admins_creater_path(@creater.id)) do |f| %>
			<p>name</p>
			<%= f.text_field :name %>
			<p>profile</p>
			<%= f.text_area :profile %>
			<p>profile</p>
			<%= f.select :status, Creater.statuses.keys{}, {class: 'form-control'} %>
			<br>
			<%= f.submit %>
			<% end %>
		</div>
	</div>
</div>