<div class="container">
  <div class="row">
    <div class="col-xs-8 col-xs-offset-2">
        <br>
        <br>
        <br>

      <h4>会員の方はこちらからログイン</h4>
      <br>
      <div class="row">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <label class="col-xs-4 control-label">メールアドレス</label>
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"col-xs-8" %>
        </div>
      </div>

      <div class="row">
        <div class="field">
          <label class="col-xs-4 control-label">パスワード </label>
          <%= f.password_field :password, autocomplete: "current-password", class:"col-xs-8" %>
        </div>
      </div>
        <div class="actions">
          <%= f.submit "ログイン",class:"btn btn-default" %>
        </div>
      <% end %>
      <%= link_to "=>パスワードを忘れた方はこちら", new_password_path(resource_name) %>

      <br>
      <br>
      <br>

      <p>登録がお済でない方は<%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。</p>
    </div>
    </div>
  </div>
</div>

